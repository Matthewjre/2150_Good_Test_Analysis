# Payment Service Test Plan

| Test Number | Test Name | Acceptance Criteria | Method Input | Expected Method Output | Input State | Expected Output State |
|---|---|---|---|---|---|---|
| PAY-01 | authorizePaymentReturnsAResultForValidInput | A valid payment request should return an observable PaymentResult. | `authorizePayment("user_123", "order_456", new BigDecimal("100.00"), "USD")` | A non-null `PaymentResult`. | Fraud score is 10. Gateway is configured to approve with transaction ID `txn_123`. Ledger is empty. | Result object exists. Ledger state is not checked by this test. |
| PAY-02 | zeroAmountThrowsIllegalArgumentException | AC-01, AC-03 | `authorizePayment("user_123", "order_456", BigDecimal.ZERO, "USD")` | Throws `IllegalArgumentException` with message `Amount must be positive`. | Fraud client, payment gateway, and ledger are initialized but have no calls or records. | Fraud client has 0 calls. Payment gateway has 0 calls. Ledger has 0 authorization records and 0 decline records. |
| PAY-03 | approvedGatewayResponseReturnsApprovedStatus | AC-08 | `authorizePayment("user_777", "order_888", new BigDecimal("25.00"), "USD")` | A `PaymentResult` with status `approved`. | Fraud score is 15. Gateway is configured to approve with transaction ID `txn_456`. Ledger is empty. | Result status is approved. Ledger state and gateway arguments are not checked by this test. |
| PAY-04 | highFraudRiskDeclinesBeforeGatewayAuthorization | AC-04, AC-05, AC-06, AC-18 | `authorizePayment("user_123", "order_456", new BigDecimal("100.00"), "USD")` | A declined `PaymentResult` with null transaction ID and reason `fraud_risk`. | Fraud score is 95. Gateway is configured to approve if called. Ledger is empty. | Fraud client has 1 call. Gateway has 0 calls. Ledger has 1 decline for `order_456` with reason `HIGH_FRAUD_RISK` and 0 authorizations. |
| PAY-05 | positiveAmountDoesNotThrowException | A valid positive amount should not be rejected as invalid. | `authorizePayment("user_123", "order_456", new BigDecimal("100.00"), "USD")` | No exception is thrown. | Fraud score is 20. Gateway is configured to approve with transaction ID `txn_789`. Ledger is empty. | No exception occurs. Result contents and ledger state are not checked by this test. |
| PAY-06 | gatewayDeclineRecordsGatewayReason | AC-12, AC-13, AC-14, AC-15 | `authorizePayment("user_123", "order_456", new BigDecimal("100.00"), "USD")` | A declined `PaymentResult` with null transaction ID and reason `INSUFFICIENT_FUNDS`. | Fraud score is 10. Gateway is configured to reject with reason `INSUFFICIENT_FUNDS`. Ledger is empty. | Gateway has 1 call. Ledger has 1 decline for `order_456` with reason `INSUFFICIENT_FUNDS` and 0 authorizations. |
| PAY-07 | fraudClientIsCalledForValidPayment | FR-02 | `authorizePayment("user_123", "order_456", new BigDecimal("100.00"), "USD")` | No return value is asserted. | Fraud score is 10. Gateway is configured to approve with transaction ID `txn_123`. Ledger is empty. | Fraud client call count is greater than 0. Result contents, gateway behavior, and ledger state are not checked by this test. |
| PAY-08 | fraudScoreAtThresholdIsDeclined | AC-07 | `authorizePayment("user_123", "order_456", new BigDecimal("100.00"), "USD")` | A declined `PaymentResult` with reason `fraud_risk`. | Fraud score is exactly 80. Gateway is configured to approve if called. Ledger is empty. | Gateway has 0 calls. Ledger has 1 decline with reason `HIGH_FRAUD_RISK` and 0 authorizations. |
| PAY-09 | declinedPaymentHasDeclinedStatus | AC-04 | `authorizePayment("user_123", "order_456", new BigDecimal("100.00"), "USD")` | A `PaymentResult` with status `declined`. | Fraud score is 90. Gateway is configured to approve if called. Ledger is empty. | Result status is declined. Gateway call count and ledger state are not checked by this test. |
| PAY-10 | approvedPaymentRecordsAuthorizationWithTransactionDetails | AC-08, AC-09, AC-10, AC-11, AC-16 | `authorizePayment("user_123", "order_456", new BigDecimal("100.00"), "USD")` | An approved `PaymentResult` with transaction ID `txn_789` and null reason. | Fraud score is 20. Gateway is configured to approve with transaction ID `txn_789`. Ledger is empty. | Gateway has 1 call with order ID `order_456`, amount `100.00`, and currency `USD`. Ledger has 1 authorization with the expected order ID, transaction ID, amount, and currency, and 0 declines. |
